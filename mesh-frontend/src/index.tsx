import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

window.document.title = 'Mesh Frontend'

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
